1)Anagram :
s="nagaram" t="anagram" in both s & t  a-3 , n -1 , g-1, ,m-1 ,r-1 so thet are anagrams

brute :
class Solution {
    public boolean isAnagram(String s, String t) {
        if(s.length() != t.length()){
            return false;
        }
        HashMap  <Character,Integer> sm =  new HashMap<>();
        HashMap  <Character,Integer> tm =  new HashMap<>();
        for(int i=0;i<s.length();i++){
            sm.put(s.charAt(i),sm.getOrDefault(s.charAt(i),0)+1);
        }
        for(int j=0;j<t.length();j++){
            tm.put(t.charAt(j),tm.getOrDefault(t.charAt(j),0)+1);
        }
        return sm.equals(tm);     
    }
} 
t.c : O(n) , s.c : O(n)

optimal :  
works only for a-z:
class Solution {
    public boolean isAnagram(String s, String t) {
        if (s.length() != t.length()) {
            return false;
        }
        int[] freq = new int[26];
        for (int i = 0; i < s.length(); i++) {
            freq[s.charAt(i) - 'a']++;
            freq[t.charAt(i) - 'a']--;
        }
        for (int count : freq) {
            if (count != 0) {
                return false;
            }
        }
        return true;
    }
}
works for A-Z:

for (int i = 0; i < s.length(); i++) {
            freq[s.charAt(i) - 'A']++;
            freq[t.charAt(i) - 'Z']--;
}

works for a-z A-Z  white space :       
int[] freq = new int[128];
for (int i = 0; i < s.length(); i++) {
            freq[s.charAt(i)]++;
            freq[t.charAt(i)]--;
        }
----------------------------------------------------------------------------------------------------------------------------------------
2)Largest Odd Number ins string: e.g : String num = 576754 largerst odd number is 57675 , num = 52 return 5 num=6 return "" coz no long odd number found 
optimal :
class Solution {
    public String largestOddNumber(String num) {
         for(int i =num.length()-1;i>=0;i++){
            if((num.charAt(i) - '0') % 2 != 0){
              return num.substring(0,i+1);
            }
         } 
             return "";   
    }
}
t.c : O(n) ,s.c : O(1)

  
