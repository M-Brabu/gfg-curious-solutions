----------------------------------------------------------------------------------------------------------
1)max and min in array:

class Solution {
    public ArrayList<Integer> getMinMax(int[] arr) {
        ArrayList <Integer> res =new ArrayList<>();
        int min=arr[0];
        int max=arr[0];
        for(int i=0;i<arr.length;i++){
            if(arr[i]<min){
                min=arr[i];
            }
            if(arr[i]>max){
                max=arr[i];
            }
        }
        res.add(min);
        res.add(max);
        return res;
    }
}
------------------------------------------------------------------------------------------------------
2)sorting array (genric way):

for (int i = 0; i < arr.length - 1; i++) {
    for (int j = i + 1; j < arr.length; j++) {
        if (arr[i] > arr[j]) {
            int temp = arr[i];
            arr[i] = arr[j];
            arr[j] = temp;
        }
    }
}
-------------------------------------------------------------------------------------------------------
3) third largest number in array : (without repeatation) eg:3,4,7,5   ans: 4 , 3,4,5,5 ans:3

class Solution {
    public int thirdLargest(List<Integer> arr) {
        Integer first = null;
        Integer second = null;
        Integer third = null;
        if(arr.size()<3){
            return -1;
        }
        for(Integer num : arr){
            if(first!=null && num.equals(first) || second!=null && num.equals(second) || third!=null && num.equals(third)){
                continue;
            }
            if(first ==null|| num > first){
                third=second;
                second=first;    
                first=num;           
            }
            else if(second ==null ||num > second){
                third =second;
                second = num;
            }
            else if(third == null || num > third){
                third = num;
            }
        }
    return third;    
    }
}

with repetation allowed : eg : 5,5,5  ans: 5

class Solution {
    public int thirdLargest(List<Integer> arr) {
        Integer first = null;
        Integer second = null;
        Integer third = null;
        
        if(arr.size()<3){
            return -1;
        }
        for(Integer num : arr){
            if(first ==null|| num > first){
                third=second;
                second=first;    
                first=num;        
            }
            else if(second ==null ||num > second){
                third =second;
                second = num;
            }
            else if(third == null || num > third){
                third = num;
            }
        }
    return third;    
    }
}
leetcode :
[3,4,5,5,5] - 3 ,[1,2] - 2 co thier is no max third number so return the maximum number
class Solution {
    public int thirdMax(int[] nums) {
        int n = nums.length;
        long first = Long.MIN_VALUE;
        long second = Long.MIN_VALUE;
        long third = Long.MIN_VALUE;
        if(n<3){
            if(n==2){
                return Math.max(nums[0],nums[1]);
            }
            else{
                return nums[0]; 
            }
        }
        for(int i=0;i<n;i++){
            if(nums[i]==first ||nums[i]==second ||nums[i]==third){
                    continue;
            }
            else{
                if(nums[i]>first){
                    third =  second;
                    second = first;
                    first = nums[i];
                }
                else if(nums[i]>second){
                    third= second;
                    second = nums[i];
                }
                else if(nums[i]>third){
                    third = nums[i];
                }
            }
        }
        if(third == Long.MIN_VALUE){
           return (int)first; 
        }
        return (int)third;
    }
}
------------------------------------------------------------------------------------------------------
4)finding missing number in array :
Examples:
Input: arr[] = [1, 2, 3, 5]
Output: 4
Explanation: All the numbers from 1 to 5 are present except 4.
Input: arr[] = [8, 2, 4, 5, 3, 7, 1]
Output: 6
Explanation: All the numbers from 1 to 8 are present except 6.
Input: arr[] = [1]
Output: 2
Explanation: Only 1 is present so the missing element is 2.

class Solution {
    long missingNum(int arr[]) {
        // code here
        long n = arr.length+1;
        long sum = 0;
        for(int i=0;i<arr.length;i++){
            sum = sum+arr[i];
        }
        long formula_sum=(n*(n+1))/2;
        long missing =formula_sum - sum;
        return missing;
    }
}
---------------------------------------------------------------------------------------------------------
5)rotate array by 1 time :   (shifting method)
input : [1,2,3,4,5] 
output : [5,1,2,3,4]
T.C : O(n)
S.C : O(1)

class Solution {
    public void rotate(int[] arr) {
        // code here
        int last_element = arr[arr.length-1];
        for(int i=arr.length-1;i>0;i--){
            arr[i]=arr[i-1];
        }
        arr[0]=last_element;
    }
}

------------------------------------------------------------------------------------------------------------
6) rotate array by k : (reveral algorithm)  

left rotation:                   [1,2,3,4,5] k =2   1)[2,1,3,4,5]  2)[2,1,5,4,3] 3)[3,4,5,1,2]
--------------
import java.util.ArrayList;
public class Solution {
public static void reverse(ArrayList<Integer> arr, int s, int e) {
        while (s < e) {
            int temp = arr.get(s);
            arr.set(s, arr.get(e));
            arr.set(e, temp);
            s++;
            e--;
        }
    }
    public static ArrayList<Integer> rotateArray(ArrayList<Integer> arr, int k) {
        int n = arr.size();
        k = k % n;
        reverse(arr, 0, k - 1);
        reverse(arr, k, n - 1);
        reverse(arr, 0, n - 1);
        return arr;
    }
}
 
right rotation :                 [1,2,3,4,5] k =2   1)[5,4,3,2,1]  2)[4,5,3,2,1] 3)[4,5,1,2,3]
-----------------
 reverse(arr, k, n - 1);
 reverse(arr, 0, n - 1);
 reverse(arr, 0, k - 1);
------------------------------------------------------------------------------------------------------------------------------------
7)Array subset e.g: a=[1,2,3,4,5] b=[3,4] b is sub set of a ; a=[1,2,3,3] b=[1,2,3] b is not subset of a because a has 2 occurence of 3 but b has 1


import java.util.HashMap;
class Solution {
    public boolean isSubset(int a[], int b[]) {
        // Your code here
        HashMap <Integer,Integer> map = new HashMap<>();
        //frequency of numbers in array
        for(int num : a){
            map.put(num,map.getOrDefault(num,0)+1);
        }
        for(int subnum :b){
            if(!map.containsKey(subnum) || map.get(subnum) == 0){
                return false;
            }
            map.put(subnum,map.get(subnum)-1);
        }
        return true;
    }
}

----------------------------------------------------------------------------------------------------------
8) check if arrays are equal [1,2,2,3]==[3,1,2,2]  , [1,2,2]!=[1,2]

class Solution {
    public static boolean checkEqual(int[] a, int[] b) {
     if(a.length != b.length){
         return false;
     }
     HashMap <Integer,Integer> map = new HashMap <>();
     for(int Anum : a){
        map.put(Anum,map.getOrDefault(Anum,0)+1);
     }
     for(int Bnum :b){
         if(map.containsKey(Bnum)==false){
             return false;
         }
         map.put(Bnum,map.get(Bnum)-1);
         if(map.get(Bnum)==0){
             map.remove(Bnum);
         }
    }
    return map.isEmpty();
    }
}
---------------------------------------------------------------------------------------------------------------------------------
9) Check if array is sorted [1,2,3,4,5] true  , [2,5,3,1,3] false   (striver)
class Solution {
    public boolean isSorted(ArrayList<Integer> nums) {
                for(int i=nums.size()-1;i>0;i--){
                    if(nums.get(i)>nums.get(i-1)){
                          continue;
                    }
                    return false;
                }
                return true;
    }
}
-------------------------------------------------------------------------------------------------------------------------------
10)Remove duplicats elements in an array :                       (striver)
class Solution {
    public int removeDuplicates(int[] nums) {
        int n = nums.length;
        int i=0;
        for(int j=1;j<n;j++){
          if(nums[i]!=nums[j]){
            i++;
            nums[i]=nums[j];
          }
        }
        return i+1;             
    }
}
Array: 1 1 2 2 3 4 4

Initial:
i = 0, j = 1
j = 1
1 == 1 → Duplicate → Do nothing
j = 2
1 != 2 → i++ → nums[i] = 2
Array: 1 2 2 2 3 4 4
j = 3
2 == 2 → Duplicate → Do nothing
j = 4
2 != 3 → i++ → nums[i] = 3
Array: 1 2 3 2 3 4 4
j = 5
3 != 4 → i++ → nums[i] = 4
Array: 1 2 3 4 3 4 4
j = 6
4 == 4 → Duplicate → Do nothing
Return = i + 1 = 4
Unique Elements:
1 2 3 4

Time Complexity
Best Case: O(n)
Average Case: O(n)
Worst Case: O(n)
Reason: j traverses the array only once.
Space Complexity
O(1)
Reason: Only two pointers (i, j) are used; no extra array is created
---------------------------------------------------------------------------------------------------------------------------------
11)move zeros to end of the array : (Brute force striver)

Input: nums = [0, 1, 4, 0, 5, 2]
Output: [1, 4, 5, 2, 0, 0]
Explanation:  Both the zeroes are moved to the end and the order of the other elements stay the same
class Solution {
    public void moveZeroes(int[] nums) {
        int n = nums.length;
        int zeroCount=0;
        ArrayList <Integer> dup =  new ArrayList<>(); 
        for(int i=0;i<n;i++){
            if(nums[i]!=0){
            dup.add(nums[i]);
            }
            else{
               zeroCount++;
            }
        }
        for(int j=0;j<dup.size();j++){
            nums[j]=dup.get(j);
        }
        int m = dup.size();
        while(zeroCount>0){
            nums[m]=0;
            zeroCount--;
            m++;
        }
    }
}
T.C O(n), s.c O(n)

optimal:    (Leetcode)
class Solution {
    public void moveZeroes(int[] nums) {
        int j=0;
        for(int i=0;i<nums.length;i++){
            if(nums[i]!=0){
                int temp = nums[i];
                nums[i]=nums[j];
                nums[j]=temp;
                j++;
            }
        }
    }
}
sc: O(n) , tc: O(n)
--------------------------------------------------------------------------------------------------------------------------------------
12)maximum consecutive one's:
eg:[0,1,1,0,1]   op:2  , [0,1,1,1,1,0,1,1]   op:4
class Solution {
    public int findMaxConsecutiveOnes(int[] nums) {
        int count = 0;
        int maxCount=0;
        for(int i=0;i<nums.length;i++){
               if(nums[i]==1){
                count++;
                if(count>maxCount){
                    maxCount=count;
                }
               }
               else{
                count=0;
               }
        }
        return maxCount;
}
}
t.c: O(n) , s.c : O(1)

----------------------------------------------------------------------------------------------------------------------------------------
13)Two sum Brute force : returning indices eg: [1,2,3,4] target=7 =>nums[2]+nums[3]=3+4=7 hence return arr = [2,3] 
class Solution {
    public int[] twoSum(int[] nums, int target) {
        int n = nums.length;
        int arr [] = new int[2];
         for(int i=0;i<n;i++){
            for(int j=i+1;j<n;j++){
                if(i==j){
                    continue;
                }
                if(nums[i]+nums[j]==target){
 
                    arr[0]=i;
                    arr[1]=j;
                }
            }
       
         }
              return arr;        
    }
}
T.c : o(n^2)   , s.c : o(1)  
----------------------------------------------------------------------------------------------------------------------------------------
14)Majority Element-1 e.g :[1,2,2,1,1,3,4,5]  majority element : 1 because 1 appaear 3 times :

Brute force:
import java.util.HashMap;
class Solution {
    public int majorityElement(int[] nums) {
        HashMap <Integer,Integer> map = new HashMap<>();
        for(int n : nums){
            map.put(n,map.getOrDefault(n,0)+1);
        } 
        int max = 0;
        int ans = 0;
        for(HashMap.Entry <Integer,Integer> entry : map.entrySet()){
            if(entry.getValue()>max){
                max = entry.getValue();
                ans = entry.getKey();
            }
        }
        return ans;
    }
}
T.c : o(n) ,s.c :o(n)
---------------------------------------------------------------------------------------------------------------------------------------
15)Single element e.g :[1,1,2,2,3,4,4,5,5]   ans : 3 becase all other elements in array appears twice except 3 so 3 is single element:
Brute Force
class Solution {
    public int singleNumber(int[] nums) {
        HashMap <Integer,Integer>  map = new HashMap<>();
        for(int n : nums){
            map.put(n,map.getOrDefault(n,0)+1);
        }
        for(HashMap.Entry <Integer,Integer> entry : map.entrySet()){
            if(entry.getValue()==1){
                return entry.getKey();
            }
        }
        return -1;
    }

t.c : O(n), s.c : O(n)
--------------------------------------------------------------------------------------------------------------------------------------
16)longest sub array with sum k ----e.g : [1,2,1,1,1,3] if k=3   then [1,1,1] is longest sub array with sum == k  second largest [1,2]
Brute :
class Solution {
    public int longestSubarray(int[] nums, int k) {

      int len = 0;
      int n = nums.length;      
      for(int i=0;i<n;i++){
        int sum = 0; 
        for(int j=i;j<n;j++){
            sum = sum + nums[j];
            if(k == sum){
                 len = j-i+1 >len ? j-i+1: len;
            }
        }
      }
      return len;  
    }
}
t.c O(n^2) , O(1)
---------------------------------------------------------------------------------------------------------------------------------------
17)sort 0s 1s 2s : sort colors : medium
brute force :
        int n=  nums.length;
        int count0 = 0;
        int count1 = 0;
        int count2 = 0;
        for(int i=0 ; i<n ; i++ ){
           if(nums[i]==0){
            count0++;
           }
           else if(nums[i]==1){
            count1++;
           }
           else{
            count2++;
           }
        }
        int j;
        for(j=0; j<count0;j++){
             nums[j]=0;
        }              
        for(j=count0 ; j<count0+count1 ;j++ ){
            nums[j]=1;
        }
        for(j=count0+count1;j<n;j++){
            nums[j]=2;
        } 
Approach   Time	   Space   Passes
Counting 	O(n)	O(1)	2

optimal:
Dutch National Flag:
class Solution {
    public void sortColors(int[] nums) {
        int n=  nums.length;
        int low =0;
        int mid =0;
        int high =n-1;
        int temp;
        while(mid<=high){
            if(nums[mid] == 0 ){
                temp=nums[mid];
                nums[mid]=nums[low];
                nums[low]=temp;
                low++;
                mid++;
            }
            else if(nums[mid] == 1){
                mid++;
            }
            else{
                //nums[mid] == 2
                temp=nums[mid];
                nums[mid]=nums[high];
                nums[high]=temp;
                high--;
            }
        }      
    }
}
    
T.C:O(n) , S.C:O(1)    passes : 1 
--------------------------------------------------------------------------------------------------------------------------------------
18) Maximum sub array sum:   e.g : [1,-2,3,4]    subarray : [3,4] = 7 is maximum sub array others [1] = 1 [1,-2]=-1 ,[1,-2,3]=2 ,[1,-2,3,4] = 6 
[-2,3] = 1,[-2,3,4]=5,[3,4]=7,[4]=4 
brute:
class Solution {
    public int maxSubArray(int[] nums) {
        int n = nums.length;
        int sum = 0;
        int max = -99999;
        for(int i=0;i<n;i++){
            sum = 0;
            for(int j=i;j<n;j++){
                sum = sum + nums[j];
                if(max<sum){
                    max=sum;
                }
            }
        }
          return max;
    }
}
t.c : O(n^2)  , s.c : O(1)

optimal:
Kadane's:        e.g : [1,-2,3,4]  curentsum = 1 -> -1 -> reset to 0 coz curent sum goes to -ve -> 3 -> 7 so [3,4] = 7
         for(int num : nums){
            currentSum += num;
            // Update maximum sum if current sum is larger
            if (currentSum > maxSum) {
                maxSum = currentSum;
            }
            // If current sum becomes negative,
            // discard it and start a new subarray
            if (currentSum < 0) {
                currentSum = 0;
            }
        }
t.c: O(n)  ,s.c : O(1)
----------------------------------------------------------------------------------------------------------------------------------------
19) Best Time to Buy and Sell Stock :
class Solution {
    public int maxProfit(int[] prices) {
     int maxProfit = 0;
     int buy = Integer.MAX_VALUE;
     for(int i=0;i<prices.length;i++){
        if(prices[i]<buy){
            buy=prices[i];
        }
        else{
            int current_profit = prices[i]-buy;
            if(current_profit>maxProfit){
                maxProfit = current_profit;
            }
        }
     }
    return maxProfit;    
    }
}

Time Complexity: O(n)
Space Complexity: O(1)

Input: prices = [7,1,5,3,6,4]
Output: 5
Explanation: Buy on day 2 (price = 1) and sell on day 5 (price = 6), profit = 6-1 = 5.
Note that buying on day 2 and selling on day 1 is not allowed because you must buy before you sell.
----------------------------------------------------------------------------------------------------------------------------------------
20)Sub Array with sum equals k : (medium)
Given an array of integers nums and an integer k, return the total number of subarrays whose sum equals to k.
Example 
Input: nums = [1, 1, 1], k = 2
Output: 2
Explanation: In the given array [1, 1, 1], there are two subarrays that sum up to 2: [1, 1] and [1, 1]. Hence, the output is 2.
Brute :
class Solution {
    public int subarraySum(int[] nums, int k) {
        int n = nums.length;
        int count =0;
        for(int i=0;i<n;i++){
            int sum =0;
            for(int j=i;j<n;j++){
               sum = sum + nums[j];
               if(sum == k){
                count ++ ;
               }
            }
        }
        return count;
    }
}
t.c : O(n^2) , s.c : O(1)

---------------------------------------------------------------------------------------------------------------------------------------
21) rearrange elements by sign : e.g :[1,2,-3,4,-5,-6] -> [1,-3,2,-5,4,-6]
class Solution {
    public int[] rearrangeArray(int[] nums) {
    ArrayList <Integer> pno = new ArrayList<>();
    ArrayList <Integer> nno = new ArrayList<>();
          for(int num :nums){
            if(num>0){
                pno.add(num);
            }
            else if(num<0){
                nno.add(num);
            }
          }
          int p =0;
          int n =0;
          for(int i=0;i<nums.length;i++){
            if(i%2 == 0){
                nums[i] = pno.get(p);
                p++;
            }
            else{
                nums[i] = nno.get(n);
                n++;
            }
          }
          return nums;
    }
}
✅ Time Complexity: O(n)
✅ Space Complexity: O(n)
Number of passes :2
optimal : 
class Solution {
    public int[] rearrangeArray(int[] nums) {
        int[] ans = new int[nums.length];
        int pos = 0;
        int neg = 1;
        for (int num : nums) {
            if (num > 0) {
                ans[pos] = num;
                pos += 2;
            } else {
                ans[neg] = num;
                neg += 2;
            }
        }
        return ans;
    }
}
Time: O(n)
Space: O(n)
number of pass : 1
----------------------------------------------------------------------------------------------------------
22)leaders in array :
e.g : [5,2,1,7,6,3]  ans:[7,6,3] coz 7 left side has 6,3 smaller than 7. 6 has 3 which is smaller than 6 3 coz no elements rigth to 3 . [5] is not coz 7,6 is greated than 5.
brute :
class Solution {
    public List<Integer> leaders(int[] nums) {
       ArrayList <Integer> arr = new ArrayList<>();
       int n = nums.length;
       for(int i=0;i<n;i++){
        boolean leader = true;
        for(int j=i+1;j<n;j++){
            if(nums[i]>nums[j]){
                   continue;
            }
            else{
                 leader=false;
                 break;
            }        
        }
        if(leader == true){
            arr.add(nums[i]);
        }
       }
       return arr;   
    }
}
T.C : O(n^2) , S.C : O(n)
optimal :
class Solution {
    static ArrayList<Integer> leaders(int arr[]) {
        int n = arr.length;
        ArrayList <Integer> ans = new ArrayList<>(); 
        int maximum = Integer.MIN_VALUE;
        for(int i = n-1 ;i>=0 ; i--){
            if(arr[i]>=maximum){
                maximum = arr[i];
                ans.add(arr[i]);                
            }
        }        
       Collections.reverse(ans);
        return ans;        
    }
}
T.C : O(n) , S.C : O(n)
---------------------------------------------------------------------------------------------------------------------------------------
23)Longest consecutive sequence : e.g : [1,101,3,102,2,600,4]  sequence :[1,2,3,4] len-4 , [101,102] len-2 ,[600] len-1 so max consecutive is 4
brute:

class Solution {
    public int ls(int arr [] , int x){
        for(int i=0;i<arr.length;i++){
            if(arr[i]==x){
                return 1;
            }
        }
        return 0;
    }
    public int longestConsecutive(int[] nums) {
    int longest = 1;
    int n = nums.length;
    if(n==0){
        return 0;
    }
    for(int i=0;i<n;i++){
           int x= nums[i];
           int count = 1;
           while(ls(nums,x+1)==1){
                count=count+1;
                x=x+1;
                if(count>longest){
                    longest = count;
                }
           }
    }
    return longest;
    }
}
T.C : O(n^3) ,S.C : O(1)

optimal : 
class Solution {
    public int longestConsecutive(int[] nums) {
        int n =  nums.length;
        if(n == 0){
            return 0;
        }
        HashSet <Integer> set =  new HashSet<>();
        for(int num : nums){
            set.add(num);
        }
        int max_count = 1;
        for(int numm : set ){
        if(!set.contains(numm-1)){
          int count = 1;
          int current  = numm;
          while(current != Integer.MAX_VALUE &&set.contains(current+1)){
              count =count +1;
              current = current +1;
              if(count > max_count){
                max_count = count;
              }
          }
        }
        }
        return max_count; 
    }
}
✅ Time Complexity: O(n)
✅ Space Complexity: O(n)
--------------------------------------------------------------------------------------------------------------------------------------
24)search insert position :
e.g : [1,2,3] t=3  return 2  , [1,2,4] t=3 return 2 , [1,2,3]  t=4 return 3  
class Solution {
    public int searchInsert(int[] nums, int target) {
        for(int i=0;i<nums.length;i++){
            if(nums[i]>=target){
                return i;
            }
        }
        return nums.length;
    }
}
t.c : O(n) ,s.c : (1)
more optimal : O(n log n) , s.c :O(1) by using binary search 
----------------------------------------------------------------------------------------------------------------------------------------
25)Heigth checker : 
school needs students to stand in height wise : here 1,2,3 are height of students [1,2,4,3,5] but not standing in height wise but expected [1,2,3,4,5] on comapring both arr we know 2 pos 3 expected but 4 is present like wise totally 2 missorderd students heigtt

class Solution {
    public int heightChecker(int[] heights) {
        ArrayList <Integer> expected = new ArrayList<>();
        int n = heights.length;
        for(int i=0;i<n;i++){
            expected.add(heights[i]);
        }
        expected.sort(null);
        int count = 0;
        for(int j=0;j<n;j++){
                 if(expected.get(j) != heights[j]){
                    count++;
                 }
        }
        return count;
    }
}
t.c : O(n log n) ,s.c : O(n)
--------------------------------------------------------------------------------------------------------------------------------
26) contains duplicate - II:
Given an integer array nums and an integer k, return true if there are two distinct indices i and j in the array such that nums[i] == nums[j] and abs(i - j) <= k.

Example 1:

Input: nums = [1,2,3,1], k = 3
Output: true
Example 2:

Input: nums = [1,0,1,1], k = 1
Output: true
Example 3:

Input: nums = [1,2,3,1,2,3], k = 2
Output: false

Brute :   (Time exceeded in leetcode)
class Solution {
    public boolean containsNearbyDuplicate(int[] nums, int k) {
        int n =  nums.length;
        for(int i=0;i<n;i++){
            for(int j=0;j<n;j++){
                if(i!=j && nums[j] == nums[i]){
                    if(Math.abs(i-j)<=k){
                        return true; 
                    }
                }
            }
        }
        return false;
    }
}
t.c : O(n^2) , s.c :O(1)

optimal:
class Solution {
    public boolean containsNearbyDuplicate(int[] nums, int k) {
        int n = nums.length;
        HashMap <Integer,Integer> map = new HashMap<>();
        for(int i=0;i<n;i++){
            if(map.containsKey(nums[i])){
              int previndex = map.get(nums[i]);
              if(i-previndex<=k){
                return  true;
              }
            }
            map.put(nums[i],i);
        }
        return false;
    }
}

t.c : O(n) , s.c : O(n)
----------------------------------------------------------------------------------------------------------------------------------------
27)intersection of two arrays 
nums1 [1,2,3,4,5] , nums2 [2,3,4,6,7]  intersection : [2,3,4]
brute:
class Solution {
    public int[] intersection(int[] nums1, int[] nums2) {
        int n1 = nums1.length;
        int n2 = nums2.length;
        int k=0;
        ArrayList <Integer> arr = new ArrayList<>();
        for(int i=0;i<n1;i++){
            for(int j=0;j<n2;j++){
                   if(nums1[i]==nums2[j]){
                    if(!arr.contains(nums1[i])){
                    arr.add(nums1[i]);
                    k++;
                    }
                   }
               
        }
            int num[] = new int[arr.size()];
            for(int m=0;m<arr.size();m++){
                num[m]=arr.get(m);
            }
        return num;
    }
}
t.c : O(n1 × n2 × min(n1,n2)) ,s.c :O(min(n1,n2))

optimal :

class Solution {
    public int[] intersection(int[] nums1, int[] nums2) {
        HashSet <Integer> set = new HashSet<>();
        HashSet <Integer> result = new HashSet<>();
        for(int num1 : nums1 ){
            set.add(num1);
        }
        for(int num2 : nums2){
             if(set.contains(num2)){
                result.add(num2);
             }
        }
        int nums3 [] = new int[result.size()];
        int i=0;
        for(int num : result){
            nums3[i]=num; 
            i++;
        }
        return nums3;
    }
}


t.c : O(n1 + n2) ,s.c :O(n1 + k)
--------------------------------------------------------------------------------------------------------------------
28)Merge sorted array : [1,2,3,4]  , [2,5,6,7]  => [1,2,2,3,4,5,6,7]  
leetcode :
Input: nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3                   :. here in nums1 [1,2,3] are actual elements [0,0,0] are just empty spaces inorder to accomadate the final answer so nums1 is intially given with m+n size
Output: [1,2,2,3,5,6]
Explanation: The arrays we are merging are [1,2,3] and [2,5,6].
The result of the merge is [1,2,2,3,5,6] with the underlined elements coming from nums1.
class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        ArrayList <Integer> arr = new ArrayList<>();
        for(int j=0;j<m;j++){
            arr.add(nums1[j]);
        }
        for(int num2 : nums2){
            arr.add(num2);
        }
        Collections.sort(arr);
        for(int i=0;i<arr.size();i++){
            nums1[i] = arr.get(i);
        }
    }
}
                          Time	             Space
Your ArrayList + Sort	O((m+n) log(m+n))	O(m+n)
--------------------------------------------------------------------------------------------------------------------

29) concatenation of array : 
e g [1,2,3] -> [1,2,3,1,2,3]

class Solution {
    public int[] getConcatenation(int[] nums) {
      int n = nums.length; 
      int ans [] = new int[2*n];
      int i=0;
      for(i=0;i<n;i++){
          ans[i]=nums[i];
      }
      for(int j=0;j<n;j++){
        ans[i]=nums[j];
        i++;
      }   
      return ans;
    }
}
t.c : O(n),s.c:O(n)